server:
  port: 80

spring:
  application:
    name: demo-k8s-agent-java
  profiles:
    include:
      - knife4j

# 配置日志地址
logging:
  config: classpath:logback-spring.xml
  pattern:
    level: "%5p"
